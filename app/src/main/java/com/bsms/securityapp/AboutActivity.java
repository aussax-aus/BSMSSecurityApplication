package com.bsms.securityapp; import android.os.Bundle; import androidx.appcompat.app.AppCompatActivity; import android.widget.TextView; import android.graphics.Color; import android.view.Gravity; public class AboutActivity extends AppCompatActivity { @Override protected void onCreate(Bundle savedInstanceState) { super.onCreate(savedInstanceState); TextView tv=new TextView(this); tv.setText("About"); tv.setTextColor(Color.WHITE); tv.setTextSize(24); tv.setGravity(Gravity.CENTER); tv.setBackgroundColor(Color.parseColor("#003366")); setContentView(tv); } }